[metadata]
name = configured-sample

[options]
packages = find:
install_requires =
    requests>=2
tests_require =
    pytest>=8

[options.extras_require]
docs =
    sphinx>=8
test =
    pytest-cov>=5

[options.entry_points]
console_scripts =
    configured-sample = sample.cli:main
