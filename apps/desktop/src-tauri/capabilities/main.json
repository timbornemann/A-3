{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main window to invoke only typed A^3 health and project-open commands.",
  "windows": ["main"],
  "permissions": ["allow-open-project", "allow-query-health"]
}
