{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allows the main window to invoke only the typed health query.",
  "windows": ["main"],
  "permissions": ["allow-query-health"]
}
